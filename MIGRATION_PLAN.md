# План міграції на Telegram Mini Apps

## 📋 Огляд

Цей документ описує покроковий план переведення бота-нагадувача з класичного Telegram бота на Telegram Mini Apps.

## 🎯 Мета

Перевести інтерфейс створення/редагування нагадувань на веб-додаток, зберігши всю бекенд-логіку (база даних, планувальник).

---

## 📦 Архітектура після міграції

```
┌─────────────────┐
│  Telegram Bot   │ ← Команди (/start, /view) + відкриття Mini App
└────────┬────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌─────────────────┐  ┌──────────────────┐
│   Mini App      │  │   REST API       │
│   (Frontend)    │─▶│   (FastAPI)      │
│   HTML/CSS/JS   │  │   /api/tasks     │
└─────────────────┘  └────────┬─────────┘
                               │
                               ▼
                    ┌──────────────────┐
                    │  DatabaseManager │
                    │  ReminderManager │
                    │  (Без змін)      │
                    └──────────────────┘
```

---

## 🗂️ Нова структура проекту

```
reminderbot/
├── main.py                    # Основний файл бота (змінений)
├── bot/
│   ├── handlers.py            # Обробники команд (спрощені)
│   ├── keyboards.py           # Клавіатури (додано WebApp кнопки)
│   └── ...
├── core/
│   ├── database.py            # Без змін
│   ├── scheduler.py           # Без змін
│   └── ...
├── api/                       # НОВА ПАПКА
│   ├── __init__.py
│   ├── main.py               # FastAPI додаток
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── tasks.py          # CRUD для нагадувань
│   │   └── auth.py           # Валідація Telegram initData
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py        # Pydantic моделі
│   └── utils/
│       ├── __init__.py
│       └── telegram_auth.py # Валідація initData
├── webapp/                    # НОВА ПАПКА
│   ├── index.html
│   ├── styles.css
│   ├── app.js
│   └── components/
│       ├── task-list.js
│       ├── task-form.js
│       └── calendar.js
├── requirements.txt           # Оновлений
└── MIGRATION_PLAN.md          # Цей файл
```

---

## 📝 Покроковий план

### Етап 1: Налаштування REST API (1-2 дні)

#### 1.1 Встановити залежності
```bash
pip install fastapi uvicorn python-multipart
```

#### 1.2 Створити FastAPI додаток
- `api/main.py` - основний FastAPI додаток
- `api/routes/tasks.py` - CRUD операції для нагадувань
- `api/routes/auth.py` - валідація Telegram Web App initData
- `api/models/schemas.py` - Pydantic моделі для запитів/відповідей

#### 1.3 Endpoints API:
```
GET    /api/tasks              - Отримати всі нагадування користувача
POST   /api/tasks              - Створити нове нагадування
GET    /api/tasks/{task_id}    - Отримати конкретне нагадування
PUT    /api/tasks/{task_id}    - Оновити нагадування
DELETE /api/tasks/{task_id}    - Видалити нагадування
POST   /api/tasks/{task_id}/complete - Позначити як виконане
```

#### 1.4 Валідація Telegram initData
- Використати `hashlib` для перевірки підпису
- Витягнути `user_id` з initData
- Перевірити, що запит надійшов від Telegram

---

### Етап 2: Створення веб-додатку (2-3 дні)

#### 2.1 Базовий HTML/CSS/JS
- `webapp/index.html` - головна сторінка
- `webapp/styles.css` - стилі (адаптивний дизайн)
- `webapp/app.js` - головна логіка

#### 2.2 Інтеграція з Telegram Web App API
```javascript
// Ініціалізація
const tg = window.Telegram.WebApp;
tg.ready();
tg.expand();

// Отримання даних користувача
const user = tg.initDataUnsafe.user;
const userId = user.id;
```

#### 2.3 Компоненти:
- **TaskList** - список нагадувань
- **TaskForm** - форма створення/редагування
- **Calendar** - вибір дати (для одноразових)
- **TimePicker** - вибір часу
- **DaySelector** - вибір днів тижня

#### 2.4 Функціонал:
- Відображення списку нагадувань
- Створення нового нагадування (з усіма опціями)
- Редагування існуючого
- Видалення
- Позначення як виконане

---

### Етап 3: Інтеграція з ботом (0.5 дня)

#### 3.1 Оновити `bot/keyboards.py`
```python
from telegram import WebAppInfo, InlineKeyboardButton

WEBAPP_BUTTON = InlineKeyboardButton(
    "📱 Відкрити додаток",
    web_app=WebAppInfo(url="https://your-domain.com/webapp")
)
```

#### 3.2 Оновити `bot/handlers.py`
- Додати команду `/app` для відкриття Mini App
- Змінити `/start` - додати кнопку з WebApp
- Залишити команди `/view`, `/delete` для сумісності

#### 3.3 Оновити `main.py`
- Запустити FastAPI сервер окремо або через ASGI
- Або використати `uvicorn` окремо

---

### Етап 4: Налаштування хостингу (0.5-1 день)

#### 4.1 Варіанти хостингу:
- **Vercel/Netlify** - для статичного веб-додатку
- **Railway/Render** - для FastAPI
- **Власний сервер** - якщо є

#### 4.2 Налаштування HTTPS:
- Обов'язково HTTPS для Mini Apps
- Налаштувати домен
- Оновити URL в BotFather

#### 4.3 Налаштування в BotFather:
```
/setmenubutton
- Виберіть бота
- Вкажіть текст кнопки: "📱 Відкрити додаток"
- Вкажіть URL: https://your-domain.com/webapp
```

---

### Етап 5: Тестування (1 день)

#### 5.1 Тестування API:
- Перевірити всі endpoints
- Перевірити валідацію initData
- Перевірити обробку помилок

#### 5.2 Тестування веб-додатку:
- Відкриття з Telegram
- Створення нагадування
- Редагування
- Видалення
- Адаптивність на різних пристроях

#### 5.3 Інтеграційне тестування:
- Перевірити, що нагадування працюють
- Перевірити планувальник
- Перевірити базу даних

---

## 🔧 Технічні деталі

### Валідація Telegram initData

```python
import hashlib
import hmac
from urllib.parse import parse_qsl

def validate_init_data(init_data: str, bot_token: str) -> dict:
    """Валідує Telegram Web App initData"""
    parsed_data = dict(parse_qsl(init_data))
    hash_value = parsed_data.pop('hash', '')
    
    data_check_string = '\n'.join(
        f"{k}={v}" for k, v in sorted(parsed_data.items())
    )
    
    secret_key = hmac.new(
        "WebAppData".encode(),
        bot_token.encode(),
        hashlib.sha256
    ).digest()
    
    calculated_hash = hmac.new(
        secret_key,
        data_check_string.encode(),
        hashlib.sha256
    ).hexdigest()
    
    if calculated_hash != hash_value:
        raise ValueError("Invalid initData")
    
    return parsed_data
```

### Структура API відповіді

```python
# GET /api/tasks
{
    "tasks": [
        {
            "task_id": 1,
            "description": "Зустріч",
            "days": [0, 2, 4],
            "times": ["09:00", "18:00"],
            "interval_minutes": 30,
            "is_one_time": false,
            "one_time_date": null
        }
    ]
}

# POST /api/tasks
{
    "description": "Нове нагадування",
    "days": [0, 1, 2],
    "times": ["09:00"],
    "interval_minutes": 15,
    "is_one_time": false,
    "one_time_date": null
}
```

---

## ⚠️ Важливі моменти

1. **Безпека**: Завжди валідуйте initData на сервері
2. **HTTPS**: Обов'язково для Mini Apps
3. **CORS**: Налаштуйте CORS для API
4. **Помилки**: Обробляйте всі помилки в веб-додатку
5. **Сумісність**: Залиште старі команди для користувачів

---

## 📊 Оцінка часу

| Етап | Час | Складність |
|------|-----|------------|
| REST API | 1-2 дні | Середня |
| Веб-додаток | 2-3 дні | Середня-висока |
| Інтеграція | 0.5 дня | Низька |
| Хостинг | 0.5-1 день | Низька-середня |
| Тестування | 1 день | Середня |
| **Всього** | **5-7 днів** | **Середня** |

---

## 🚀 Наступні кроки

1. Почати з REST API (найважливіше)
2. Створити базовий веб-додаток
3. Інтегрувати з ботом
4. Налаштувати хостинг
5. Протестувати все

---

## 📚 Корисні ресурси

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

## 💡 Альтернативний підхід (поступова міграція)

Можна залишити обидва інтерфейси:
- Старий (через ConversationHandler) - для користувачів, які звикли
- Новий (Mini App) - для нових користувачів

Це дозволить:
- Поступово мігрувати користувачів
- Тестувати новий інтерфейс
- Відкотитися, якщо щось піде не так


# Reminder Bot 🤖

Telegram-бот для створення та керування нагадуваннями. Дозволяє створювати як одноразові, так і регулярні нагадування з гнучким налаштуванням розкладу.

## 🌟 Основні можливості

- **Створення нагадувань**: зручний покроковий інтерфейс.
- **Типи розкладів**:
  - Одноразові (на конкретну дату та час).
  - Регулярні (щоденно, у вибрані дні тижня або з певним інтервалом).
- **Керування**: перегляд списку активних завдань, редагування та видалення.
- **База даних**: збереження всіх завдань у SQLite для запобігання втраті даних при перезапуску бота.
- **Автоматичне відновлення**: при запуску бот автоматично відновлює всі активні нагадування з бази.

## 🛠 Технологічний стек

- **Мова**: Python 3.10+
- **Бібліотека бота**: `python-telegram-bot`
- **Планувальник**: `APScheduler`, `schedule`
- **База даних**: `aiosqlite`
- **Конфігурація**: `python-dotenv`

## 🚀 Встановлення та запуск

1. **Клонуйте репозиторій**:
   ```bash
   git clone git@github.com:AceHardwareGOK/reminderbot.git
   cd reminderbot
   ```

2. **Налаштуйте віртуальне середовище**:
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Для Linux/macOS
   # або
   .venv\Scripts\activate     # Для Windows
   ```

3. **Встановіть залежності**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Налаштуйте змінні оточення**:
   Створіть файл `.env` у кореневій папці та додайте ваш токен:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен_від_BotFather
   LOG_LEVEL=INFO
   ```

5. **Запустіть бота**:
   ```bash
   python main.py
   ```

## 📂 Структура проекту

- `main.py` — головний файл запуску.
- `bot/` — обробники повідомлень, стани діалогів та логіка інтерфейсу.
- `core/` — ядро системи: планувальник (scheduler), робота з БД та конфігурації.
- `utils/` — допоміжні функції та валідатори.
- `reminders.db` — файл бази даних SQLite.